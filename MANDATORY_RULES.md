# 小红书自动回复强制性规则 (Mandatory Rules)

## 核心强制规则 🔴 CRITICAL

### Rule 1: 真实Chrome连接（必须执行）
- **强制要求**：每一次回复都必须通过真实Chrome浏览器连接执行
- **禁止行为**：
  - ❌ 虚拟数据/模拟数据
  - ❌ Python脚本跳过
  - ❌ 批量SQL导入
  - ❌ 跳过任何步骤
- **执行流程**：
  ```
  F5 刷新 → 等待加载 → 截图识别 → 点击帖子 → 输入回复 → 点击发送 → 等待确认 → 关闭返回
  ```
- **强制执行**：每一个步骤都必须完整执行，不能简化

### Rule 2: 完整循环（必须完成指定数量）
- **循环次数**：用户指定多少个就做多少个（如：20个、50个、100个）
- **循环不中断**：
  - 不能提前停止
  - 不能部分完成（如做了5个就说完成了20个）
  - 不能用脚本跳过中间的某些
- **进度跟踪**：
  ```
  目标：20个回复
  完成：1/20 → 2/20 → 3/20 → ... → 20/20 ✅
  ```

### Rule 3: 智能浏览策略（左上角优先 + 同页扫描 + 滚动发现）
- **核心策略**：先浏览再回复，充分利用同一页面的所有帖子，而不是每次都F5刷新
- **执行规则**：

  **阶段A：首次加载**
  1. F5刷新页面，等待加载完成
  2. 截图识别当前可见的所有帖子（通常4-6个）
  3. 从左上角开始，逐个判断是否为留学相关帖子
  4. 选择最合适的帖子进行回复

  **阶段B：回复后返回同页面（关键改进）**
  5. 回复完成后，点击X关闭帖子弹窗（不要navigate回首页，不要F5刷新）
  6. 返回后页面仍在原位，继续检查同页面剩余的帖子
  7. 如果同页面还有其他留学相关帖子，直接点击下一个回复
  8. 重复直到当前可见区域的帖子都已检查/回复过

  **阶段C：向下滚动发现更多帖子**
  9. 当前可见区域没有合适帖子了 → 向下滚动一屏（scroll_down）
  10. 截图识别新出现的帖子
  11. 继续从左到右、从上到下检查和回复
  12. 可以连续滚动2-3屏寻找合适帖子

  **阶段D：刷新获取全新内容**
  13. 只有在以下情况才F5刷新：
      - 已经滚动2-3屏都没找到合适的留学帖子
      - 页面加载异常或出错
      - 连续跳过了太多不相关帖子（超过10个）

- **为什么这样更好**：
  - 真人不会每看一个帖子就刷新，而是会浏览一圈
  - 同一页面可能有2-3个好帖子，F5刷新后就看不到了
  - 向下滚动能发现更多帖子，覆盖面更广
  - 行为更接近真实用户，降低被平台识别的风险

- **帖子优先级排序**：
  - 🔴 优先级1：寻找机构/中介（直接需求，转化最高）
  - 🟠 优先级2：选校定位/背景评估（展示专业性）
  - 🟡 优先级3：专业/语言考试咨询（建立信任）
  - 🟢 优先级4：行业讨论/经验分享（品牌曝光）
  - ⚪ 跳过：非留学相关帖子

### Rule 4: 步骤完整性（零简化）
**整体流程**（浏览 + 回复 循环）：

```
========== 一轮浏览周期 ==========

步骤1: 页面准备
  - 首次/需要刷新时: F5刷新 → 等待2秒
  - 回复后继续浏览时: 不刷新，直接从当前位置继续
  - 滚动发现更多时: scroll_down → 等待1秒

步骤2: 截图扫描 (必须)
  - 执行: screenshot
  - 目的: 识别当前可见的所有帖子（通常4-6个）
  - 判断: 从左上角开始，逐个检查哪些是留学相关
  - 记住: 哪些帖子已经看过/回复过/跳过了

步骤3: 选择目标帖子 (必须)
  - 从当前可见帖子中选择最合适的（未回复过的）
  - 优先级: 寻找机构 > 选校定位 > 专业咨询 > 行业讨论
  - 如果当前屏幕没有合适的:
    → 先向下滚动（scroll_down）查看更多
    → 滚动2-3屏仍没有 → 才F5刷新

========== 单个回复流程 ==========

步骤4: 点击帖子 (必须)
  - 执行: 点击目标帖子
  - 等待: 2秒详情页加载

步骤5: 识别并激活评论输入框 (必须) ⚠️ 用JS focus
  - ✅ 执行: JS `document.querySelector('#content-textarea').click(); .focus();`
  - ❌ 不要直接点击页面输入框（可能点到子回复框导致焦点错位）
  - 注意: 如果输入框有残留内容，先Ctrl+A → Backspace清除

步骤6: 输入回复内容 (必须)
  - 执行: type "回复文本"
  - 长度: 遵循prompt规范（≥80字）

步骤7: 截图确认 (必须) ⚠️⚠️⚠️ 关键验证
  - 执行: screenshot
  - ✅ 必须确认: 输入框中显示了完整回复文本
  - ❌ 如果看到占位符"说点什么..." → 文本没进去，重新JS focus + type + 截图
  - ❌ 如果看到"这是一片荒地" → 输入框未激活，重新JS focus + type + 截图
  - 🔄 失败重试: JS focus → type → screenshot，最多重试2次

步骤8: 点击发送 (必须)
  - ✅ 推荐: 用find定位"发送"按钮ref → 点击ref（最可靠）
  - 等待: 2秒提交确认
  - ✅ 发送成功标志: 页面返回feed 或 评论区出现自己的评论
  - ❌ 发送失败标志: 仍在详情页且输入框为空 → 重新执行步骤5-8
  - ⚠️ 只有确认发送成功后才能进入步骤9

步骤9: 返回feed (必须) ⚠️ 关键改进
  - ✅ 优先方式: 点击左上角X关闭帖子弹窗（保持页面位置不变）
  - ⚠️ 备用方式: 如果X关闭失败，才用navigate回首页
  - ❌ 不要F5刷新（除非需要获取全新内容）

步骤10: 记录到Excel (必须)
  - 执行: 追加到 {SKILL_DIR}/xiaohongshu_replies_log.xlsx（同一个文件，不创建新文件）
  - 必须记录的完整内容：
    * 序号（自动递增：1, 2, 3...）
    * 时间戳（回复发送的准确时间）
    * 帖子类型（如：27fall规划、GPA评估等）
    * 帖子标题（从Chrome页面识别到的完整标题）
    * 帖子正文内容（从Chrome页面识别到的完整正文）
    * 帖子链接（点击进帖子后，浏览器地址栏的完整URL，包含所有参数）
    * 回复内容（在Chrome中实际发送的完整回复文本）
    * 模型标注（Claude 或 Haiku）
    * [可选] 帖子质量评分（0-100分）
  - ⚠️ 立即记录：完成此步骤后才能进行下一个回复

========== 继续下一个回复 ==========

[返回步骤2: 截图扫描同页面剩余帖子，而不是步骤1刷新]
[只有当同页面+滚动都没有合适帖子时，才回到步骤1 F5刷新]
```

**浏览策略示意图**：
```
F5刷新
  ↓
截图识别第一屏 → 帖子A(留学) → 回复 → 关闭X返回
  ↓                                        ↓
                              同页面继续 → 帖子B(非留学,跳过)
                                           ↓
                                        帖子C(留学) → 回复 → 关闭X返回
                                           ↓
                              同页面没有了 → scroll_down滚动
                                           ↓
                              截图识别新帖子 → 帖子D(留学) → 回复...
                                           ↓
                              滚动2-3屏没有 → F5刷新获取新内容
```

### Rule 5: 禁止偷懒行为 🚫🚫🚫 CRITICAL

**以下行为严格禁止**：

| 禁止行为 | 原因 | 替代方案 | 严重性 |
|---------|------|--------|---------|
| 🔴 **编写Python脚本批量执行回复** | 完全违反真实Chrome要求,是最严重的作弊行为 | 100%通过Chrome手动执行每一个回复 | **绝对禁止** |
| 🔴 **使用Python脚本自动添加到Excel** | 违反真实Chrome要求,数据造假 | 每次Chrome回复后立即用Python追加单条记录 | **绝对禁止** |
| 🔴 **创建自动化脚本替代Chrome操作** | 绕过真实浏览器执行,失去skill核心价值 | 每个步骤都必须真实执行 | **绝对禁止** |
| 🔴 **编写批量导入/虚拟数据脚本** | 数据造假,欺骗用户 | 只记录真实回复的数据 | **绝对禁止** |
| 🟠 部分做Chrome+部分脚本 | 混合作弊,半真半假 | 全部Chrome操作 | 严重违规 |
| 🟠 跳过某些回复 | 没有完成指定数量 | 每个都完整做 | 严重违规 |
| 🟠 简化步骤（如不截图直接点击） | 无法验证执行 | 按照12个步骤全部做 | 严重违规 |
| 🟡 提前报告"完成" | 未真实完成 | 等到真正完成才报告 | 一般违规 |

**🔴 特别强调 - 关于Python脚本的使用边界**：

**✅ 允许的Python使用**（仅限单条记录追加）：
```python
# 只允许：在Chrome中完成单次回复后,立即追加单条记录到Excel
df = pd.read_excel(excel_path)
new_row = {...}  # 单条记录
df = pd.concat([df, pd.DataFrame([new_row])], ignore_index=True)
df.to_excel(excel_path, index=False)
```

**❌ 绝对禁止的Python使用**（批量/自动化）：
```python
# 禁止：循环批量添加记录
for i in range(20):
    new_row = {...}
    df = pd.concat([df, pd.DataFrame([new_row])], ignore_index=True)

# 禁止：自动化脚本执行回复
def auto_reply_batch():
    for post in posts:
        # 任何形式的自动化循环
        ...

# 禁止：虚拟数据生成
new_row = {
    '帖子标题': '虚构的标题',
    '帖子链接': '虚构的链接',
    ...
}

# 禁止：任何试图绕过Chrome真实操作的脚本
```

**违反此规则的后果**：
- ❌ 立即停止执行
- ❌ 删除所有通过脚本生成的虚假数据
- ❌ 要求重新从头开始真实执行
- ❌ 用户有权拒绝接受任何"效率优化"的理由

**为什么如此严格**：
1. **真实性是核心价值**：如果可以用脚本,为什么还需要skill?
2. **数据可信度**：用户需要真实的回复数据来分析优化
3. **平台风险**：脚本行为容易被平台识别封号
4. **skill存在意义**：绕过Chrome = 违背skill的根本目的

### Rule 6: 验证机制 + 发送失败恢复
**每个回复必须验证**：
- ✅ 实际点击了帖子（有URL变化）
- ✅ 实际输入了文本（screenshot显示input box有内容）
- ✅ 实际发送了（等待2秒后返回feed）
- ✅ 已记录到Excel（文件有新行）

**⚠️ 输入验证（防止焦点错位导致发送失败）**：
- 输入文本后**必须截图确认**文本确实出现在正确的输入框中
- 如果截图显示"说点什么..."占位符或"这是一片荒地" → 文本没有进入输入框
- 原因：页面中可能存在多个输入框（主评论框 vs 子回复框），直接click可能焦点错位
- 解决：始终用 `JS: document.querySelector('#content-textarea').click(); .focus();` 激活输入框

**🔄 发送失败恢复流程**：
```
发送失败（点击发送后仍在详情页、输入框为空、无新评论出现）
  ↓
恢复1: 重新JS focus输入框
  ↓
恢复2: 重新type回复内容
  ↓
恢复3: 截图确认文本在输入框中（不可跳过）
  ↓
恢复4: 用find ref方式点击发送
  ↓
恢复5: 等待2秒验证
  ↓
如果仍然失败（连续2次）→ 关闭帖子，标记"发送失败"，继续下一个
```

**⚠️ 关键教训**：
- Excel记录必须在**确认发送成功后**才写入，不能先记录再验证
- type命令返回"Typed..."不代表文本进入了正确的输入框，必须截图验证

### Rule 7: 记录格式（强制）

**Excel存储位置**：
```
{SKILL_DIR}/xiaohongshu_replies_log.xlsx

说明：
- {SKILL_DIR} = 当前skill所在目录（即 .claude/skills/xiaohongshu-auto-reply/）
- 所有回复都追加到这一个文件，不要创建新文件
- 每次回复完成后立即追加记录，保持数据连续性
```

**Excel必须包含的列（顺序固定）**：

```
序号 | 时间 | 帖子类型 | 帖子标题 | 帖子正文内容 | 帖子链接 | 回复内容 | 模型 | 质量评分 | 反馈-帖子 | 反馈-回复 | 反馈原因 | 说明
```

**各列详细说明**：

| # | 列名 | 内容 | 示例 | 强制 |
|---|------|------|------|-----|
| 1 | **序号** | 自动递增(1,2,3...) | 1 | ✅ |
| 2 | **时间** | 回复发送的准确时间戳 | 2026-01-26 14:30:45 | ✅ |
| 3 | **帖子类型** | 具体分类 | 27fall规划、GPA评估、转专业等 | ✅ |
| 4 | **帖子标题** | 帖子的完整标题（从页面识别） | "GPA3.5能申什么学校？" | ✅ |
| 5 | **帖子正文内容** | 帖子识别到的完整正文内容 | "985大三学生，均分85..." | ✅ |
| 6 | **帖子链接** | 浏览器地址栏URL（完整链接） | https://www.xiaohongshu.com/explore/696ceff9... | ✅ |
| 7 | **回复内容** | 完整回复文本（必须≥80字） | "你的背景不错..." | ✅ |
| 8 | **模型标注** | Claude 或 Haiku | Claude | ✅ |
| 9 | **帖子质量评分** | 0-100分（可选） | 85 | ⚠️ 可选 |
| 10 | **你的反馈-帖子** | YES ✅ 或 NO ❌ | YES | ⚠️ 可选 |
| 11 | **你的反馈-回复** | YES ✅ 或 NO ❌ | YES | ⚠️ 可选 |
| 12 | **反馈原因** | 多选原因 | 太长/语气不当等 | ⚠️ 可选 |
| 13 | **说明** | 用户反馈文字说明 | "建议更简洁" | ⚠️ 可选 |

**强制要求**：
- ✅ 前7列必须完整填充（序号、时间、类型、标题、正文、**链接**、回复）
- ✅ 每行必须真实数据（不能虚构）
- ✅ 帖子标题 = 从Chrome页面识别到的完整标题
- ✅ 帖子正文 = 从Chrome页面识别到的完整正文内容
- ✅ **帖子链接 = 点击进帖子后浏览器地址栏的完整URL**（包含所有参数）
- ✅ 回复内容 = 与Chrome中实际发送的完全一致
- ✅ 时间戳必须准确（不能篡改）
- ✅ 每个回复完成后立即记录（不能最后一起补）
- ❌ 禁止删除或修改已记录的行
- ❌ 禁止使用脚本或虚拟数据填充

---

### Rule 8: 直接执行，禁止规划模式（强制）

**本skill是执行型任务，不是规划型任务**

- ❌ 禁止在收到回复请求时进入Plan Mode
- ❌ 禁止创建implementation plan
- ❌ 禁止使用Plan agent
- ✅ 立即开始执行12步循环
- ✅ 重复N次直到完成所有回复

**何时可以进入Plan Mode:**
- 仅当用户明确要求"规划"、"设计"、"修改skill本身"时
- 不是一个正常的批量回复请求

**正常回复请求的正确行为:**
```
用户："继续回复10个"
→ Claude: 立即开始执行Reply 1/10的12步循环
→ 不创建plan，不进入plan mode，直接执行
→ Reply 2/10...
→ 完成全部10个回复
```

**错误的行为（要拒绝）:**
```
用户："继续回复10个"
→ Claude: "让我进入plan mode规划这个任务..." ❌ 错误！
→ Claude: "创建implementation plan..." ❌ 错误！
→ Claude: "我需要设计如何完成这个任务..." ❌ 错误！
```

**理由**：
- 12步循环已经是最佳的"plan"
- 没有需要planning的设计决策
- 这是重复执行相同的workflow，不是设计新功能
- 违反此规则 = 浪费用户时间，违反整个skill的核心目的

---

## 用户的强制权限

如果Claude Code违反上述任何规则：
- ❌ 不接受任何理由（"太慢了"、"为了效率"等）
- ❌ 不接受任何变通方案（混合方案、部分真实等）
- ❌ 不接受任何简化（删除步骤、跳过截图等）
- ✅ 用户有权叫停并要求重新做
- ✅ 用户有权要求逐步验证每个回复

---

## 执行清单

使用这个清单检查每个回复是否真实执行：

```
回复 #N:
[ ] F5刷新并等待（或同页面继续）
[ ] 截图看到帖子列表
[ ] 点击帖子进入详情页
[ ] 等待详情页加载
[ ] 🔴 用JS focus激活评论输入框（document.querySelector('#content-textarea').click(); .focus();）
[ ] 输入回复文本（type命令）
[ ] 🔴 截图验证文本确实在输入框中（不是占位符、不是空白）
[ ] 用find ref点击发送按钮
[ ] 等待提交确认 + 🔴 验证发送成功（返回feed或评论区出现新评论）
[ ] 返回到feed
[ ] 🔴 确认发送成功后才记录到Excel（不能先记录再验证）
[ ] 🔴 执行随机等待（5-10秒基础 + N%3/5/10额外等待）
[ ] 🔴 生成随机数(1-100)，执行模拟行为（点赞/收藏/滚动/看评论/无）
```

---

### Rule 9: 节奏控制（必须执行）

**目的**：模拟真人浏览行为，降低被平台识别为机器人的风险

#### A. 随机等待机制

在步骤10（记录Excel）完成后，步骤2（截图扫描下一个）之前，执行等待：

| 触发条件 | 等待时间 | 说明 |
|---------|---------|------|
| 每次回复后 | 5-10秒随机 | 基础间隔 |
| 每3个回复后 (N % 3 == 0) | 额外15-30秒 | 模拟思考 |
| 每5个回复后 (N % 5 == 0) | 额外30-60秒 | 短休息 |
| 每10个回复后 (N % 10 == 0) | 额外2-3分钟 | 长休息 |

**执行方式**：使用 Bash timeout 命令等待
```bash
# 示例：等待8秒
timeout /t 8 /nobreak
```

#### B. 模拟真人浏览行为（真实执行）

每次回复后，生成随机数(1-100)决定是否触发额外行为：

| 随机数范围 | 行为 | 触发概率 |
|-----------|------|---------|
| 1-10 | 点赞一个非留学帖子 | 10% |
| 11-15 | 收藏一个帖子但不回复 | 5% |
| 16-23 | 纯滚动浏览2-3屏 | 8% |
| 24-28 | 打开一个帖子看评论后关闭 | 5% |
| 29-100 | 不触发额外行为 | 72% |

**总触发率约28%**

**各行为执行细节**：

**点赞操作**：
1. 在feed页面找到一个非留学相关帖子（美食/穿搭/娱乐等）
2. 直接点击帖子卡片上的爱心图标（不用打开帖子）
3. 等待1-2秒
4. 继续正常流程

**收藏操作**：
1. 点击一个帖子打开
2. 点击收藏按钮（五角星图标）
3. 等待1-2秒
4. 点击X关闭
5. 继续正常流程

**滚动浏览**：
1. 在feed页面执行scroll_down 2-3次
2. 等待2-3秒（模拟阅读）
3. 继续正常流程

**查看评论**：
1. 点击一个帖子打开
2. 在评论区滚动1-2次
3. 等待3-5秒（模拟阅读评论）
4. 点击X关闭
5. 继续正常流程

#### C. 模拟行为统计记录

**统计文件**：`{SKILL_DIR}/simulation_behavior_log.xlsx`

**在每次session结束时（完成全部回复或手动停止时）**，记录一行汇总：

| 列名 | 说明 | 示例 |
|------|------|------|
| 日期 | YYYY-MM-DD | 2026-02-05 |
| Session开始时间 | HH:MM:SS | 14:30:00 |
| Session结束时间 | HH:MM:SS | 15:05:32 |
| 回复总数 | 本次session完成的回复数 | 20 |
| 点赞次数 | 触发点赞的次数 | 2 |
| 收藏次数 | 触发收藏的次数 | 1 |
| 滚动次数 | 触发纯滚动的次数 | 3 |
| 查看评论次数 | 触发查看评论的次数 | 1 |
| 总等待时长(秒) | 所有等待时间累计 | 245 |

**记录时机**：
- 仅在session结束时记录一行（不是每个回复都记录）
- 在内存中累计各项计数
- 最后追加到Excel

#### D. 完整的单次回复流程（含节奏控制）

```
步骤1-10: [原有的12步流程]

步骤10.5: 节奏控制（新增）
  a) 生成随机数(1-100)
  b) 根据随机数决定是否触发模拟行为
  c) 如果触发：执行对应行为（点赞/收藏/滚动/看评论）
  d) 累计该行为计数器 +1
  e) 计算等待时间：
     - 基础等待：random(5-10)秒
     - 如果当前回复数 N % 3 == 0：额外 random(15-30)秒
     - 如果当前回复数 N % 5 == 0：额外 random(30-60)秒
     - 如果当前回复数 N % 10 == 0：额外 random(120-180)秒
  f) 执行等待
  g) 累计总等待时长

步骤11: 继续下一个回复（原步骤2）

[Session结束时]
步骤12: 记录模拟行为统计到 simulation_behavior_log.xlsx
```

---

## 强制性说明

**这些规则是强制的（MANDATORY）**，不可协商，不可省略。

违反这些规则的任何尝试都将被拒绝，并要求重新执行。

用户优先。真实优先。完整优先。
